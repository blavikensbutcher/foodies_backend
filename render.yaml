services:
  - type: web
    name: foodies-backend
    runtime: node
    plan: free
    buildCommand: yarn install && npx prisma generate && npx prisma migrate deploy && yarn build
    startCommand: yarn start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_VERSION
        value: 24.9.0
